# Le chipset

Le chipset envoie les informations à tous les composants un peu 'secondaire' qui ne sont pas le processeur ou le GPU.

C'est un assistant processeur qui gère le flux de donnée que le processeur envoie.
